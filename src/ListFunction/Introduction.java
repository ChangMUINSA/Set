package ListFunction;

public class Introduction {
    /**
     * List接口基本介绍
     * List接口是Collection接口的子接口 List.java
     * List结合类中元素有序（即添加顺序和取出顺序一致），且可重复
     * List集合中的每个元素都有其对应的顺序索引，即支持索引
     * List容器中的元素都对应一个整数类型的序号记载其在容器中的位置，可以根据序号存取容器中的元素.
     *
     *List的实现接口 ： ArrayList LinkedList Vector
     */
}
